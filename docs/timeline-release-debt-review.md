# タイムライン リリース前 仕様精査（負債候補一覧）

作成日: 2026-09-13
最終更新: 2026-09-14（対応状況を反映）
対象: `docs-site/timeline/` 配下の仕様ドキュメント 18 ファイル（タイムライン XML version 34 時点）

リリース後は XML 形式と識別子が事実上固定されるため、**優先度 高 は version 34 を公開する前に対応可否を決める**必要がある。

**現況: 優先度 高（1〜8）・中（9〜18）・低（19〜25）とも対応方針の決定と実装が完了している。リリース前に判断が必要な負債は残っていない。**

## 優先度 高: XML 形式・識別子に関わるもの

状態欄: 対応済 = コードで解消 / 文書のみ = 仕様書に明記して許容 / 見送り = 現状維持を決定

| # | 仕様 | 問題 | 対応案 | 状態 |
|---|---|---|---|---|
| 1 | `メイド衣装` レイヤーだけメイド単位ではなく、タイムラインに 1 本で先頭スロットにしか効かない | 他のメイド系レイヤーと非対称。後からメイド単位へ変えると XML のレイヤー構造が変わりマイグレーションが必要 | メイド単位に揃える | 対応済（`093f022`） |
| 2 | モデルの対応付けが「ファイル名 + 配置順のグループ番号」 | 同じファイルを複数置くと並び替えでキーの割当先がずれる（仕様書自身が認めている） | 配置時に安定 ID を採番して保存し、それで対応付ける | 対応済（`21a824d`） |
| 3 | 階段状に切り替えるプリセットが無く、`1フレーム調整`（全体設定）でステップ補間を代替 | 後からキー単位のステップ補間を足すと、既存データが依存する全体設定と二重管理になる | キー単位のステッププリセットを追加し、`1フレーム調整` は互換読込専用へ落とす | 見送り（`1フレーム調整` のまま） |
| 4 | ポストエフェクトが区間の代表タンジェントで全項目を一括補間 | 他レイヤーは値ごとにタンジェントを持つ。後で個別化すると XML の持ち方が変わる | 値ごとのタンジェントに揃える | 対応済（`91be81a` / `32952ba`。XML 不変） |
| 5 | 保存されるが使われない値が残る: `アスペクト比` / `ﾚﾀｰﾎﾞｯｸｽ透過度`、開始・終了オフセット時間とフェード時間、PNG 配置の MTE 固有値（反転、カメラ追従、アタッチ、APNG 等） | 後で実装した瞬間に既存ファイルの見た目が変わる | 実装するか、保存対象から外すかを今決める | 対応済（`c8f8551`。読み手の無い 3 値を保存対象外へ。PNG 配置は現状維持） |
| 6 | 未使用レイヤーが保存時に空レイヤーとして実体化される | 「レイヤーが無いとゲートが出る」仕様と噛み合わない。一度保存して読み直すとゲートは二度と出ず、XML も肥大する | 空レイヤーは保存しない | 対応済（`c8a5184` + Hierarchy 選択経路 `2f31ee1` / `580fd5e`） |
| 7 | フレームレート変更でキー位置が再計算されない（仕様書に記載なし。`TimelineSettingWindow` は `frameRate` を差し替えて現在フレームを再適用するだけ） | 30→60 に変えるとアニメが 2 倍速になる | 仕様として明記するか、時間基準でキーを再配置する | 文書のみ（`b11b6bd`） |
| 8 | テンプレートの保存先が MTE と共用（`Config\MotionTimelineEditor\Template\`） | 両プラグインが同じファイルを上書きし、片方の変更が消える | SceneEditor 専用フォルダへ移し、初回だけ MTE 側から取り込む | 見送り（共用のまま） |

## 優先度 中: 挙動の意味が複雑で説明しづらいもの

| # | 仕様 | 問題 | 対応案 | 状態 |
|---|---|---|---|---|
| 9 | `登録`(Enter) の対象が「グリッドに表示中のレイヤー」 | 表示モード（カテゴリ / レイヤー）という表示状態がデータ操作の範囲を決める。`カメラ同期` OFF 時はカメラのキーが打たれない、カメラは自動登録対象外、と 3 ルールが絡む | 登録対象を明示的な概念（レイヤーごとの記録トグル等）に一本化する | 対応済（`406dc03`。編集対象レイヤー全部が対象） |
| 10 | `カメラ同期` / `ポスプロ同期` が再生反映と登録可否の両方を兼ねる | 表示制御のトグルがデータ変更にも効く | 再生反映と登録可否を分離する | 対応済（9 に含む） |
| 11 | ライブ演出（ステージライト・レーザー・サイリウム）が操作履歴非対応 | 自動登録も効かず Undo もできない、と例外が二重に生じる | 履歴対応する（両方の但し書きが消える） | 対応済（`1a73c9c` / `08767b6`） |
| 12 | Undo の穴: トラックの追加・並べ替え・範囲変更、`最終フレーム` 変更、新規作成・読込直後の最初の操作。タイムライン読込中は通常のシーン操作が履歴に載らない | 利用者からは Undo が壊れて見える | 履歴対象を広げる。シーン操作とタイムライン操作の履歴統合を検討 | 対応済（`689670c`。シーン操作が載らない件は仕様書に明記） |
| 13 | 0 フレーム目キー必須が `メイドアニメ` / `メイド表情` / `メイド瞳` / `メイド脱衣` の 4 レイヤーだけ | 保存と .anm 出力をブロックする割に、対象の選び方が説明しづらい | レイヤー作成時に 0 フレームキーを自動で打ち、制約自体を消す | 対応済（`7eb598b`） |
| 14 | レイヤー削除時の実体の扱いが不統一（`サブカメラ` は実体破棄、`背景色` は地面が残る） | 所有関係が読めない | 方針を統一する | 対応済（`1c83fd0`。「削除は実体を壊さない」に統一） |
| 15 | メイド追加後、`操作対象` で選んだ時点で「現在のレイヤー種類」だけ作られる | 増えるレイヤーがそのときの UI 状態に依存する暗黙の副作用 | 追加時に明示的に作るか、何も作らない | 対応済（優先度 高 6 で解消） |
| 16 | `メイド目線` の設定がシーンプリセット側に保存される一方、仕様上は「タイムライン全体の設定」 | 所有者が二つに分かれる | どちらかに寄せる | 文書のみ（`a492ca3`。プリセット側は「写し」と明記） |
| 17 | 補間方式のばらつき: `背景色` は色なのに補間なし（他の色は線形）、`テキスト` の Transform とサブカメラの VP は線形、他はタンジェント、PNG 配置の拡縮は X・Z のみ | 意図的でないものは仕様書を長くするだけ | 揃える | 対応済（`884497a`。`背景` の Transform はスナップのまま） |
| 18 | 回転がクォータニオンで、カーブエディタでは表示専用 | 位置は編集でき回転は編集できない非対称 | 許容するなら明記。可能ならオイラー角編集を提供 | 対応済（`5b56dba`。Euler 表示でのタンジェント編集。値編集は不可のまま） |

## 優先度 低: UX の不整合（データ互換に影響しない）

対応可否の判断と実装が済んでいる。現状維持にしたものは `docs-site/timeline/` に明記した。

| # | 仕様 | 問題 | 状態 |
|---|---|---|---|
| 19 | 破壊的操作の確認が不統一: `新規作成` / `ロード` / テンプレ削除 / トラック削除は確認なし、`アンロード` は確認あり。`連番画像出力` は同名フォルダを中身ごと削除 | 誤操作でデータ消失 | 対応済（`094a3ab`。セーブ上書き・連番画像出力のみ。残りは確認なしのまま `compatibility.md` に明記） |
| 20 | 設定が二系統: `Timeline.xml` に MTE 互換の死に項目（`EditMode` / `Undo` / `Redo`）が残り、キー割り当ても `SceneEditor.xml` と別で GUI も無い | リリース後に消すと「設定が効かなくなった」報告になる。消すなら今 | 対応済（`119a287`。`SceneEditor.xml` へ統合。旧 `keyBind` は読み飛ばすためカスタムキーは既定へ戻る） |
| 21 | ショートカットが `タイムライン操作` ウィンドウ表示中のみ有効 | ウィンドウの表示状態にキー処理が縛られる | 対応済（`50fb10a`。`TimelineKeyInput` をプラグイン本体の `Update` から呼ぶ） |
| 22 | グリッドのズームが設定ファイル編集のみ | UX | 現状維持（仕様書に明記） |
| 23 | タイムラインの削除・リネーム機能が無い。既定アニメ名 `テスト` | UX | 現状維持（仕様書に明記） |
| 24 | `個別設定を初期化` で `最終フレーム` だけ戻らない | 不整合 | 現状維持（仕様書に明記） |
| 25 | `ボイス最大秒数` / `移動範囲` という共通設定がキーの値域を決める | 設定を変えると既存データの表現範囲が変わる | 現状維持（仕様書に明記） |

## 検討結果（2026-09-13 追記）

優先度 高の 8 項目はコード調査のうえ `docs/superpowers/specs/2026-09-13-timeline-release-debt-design.md` で決定した。調査で判明した仕様書との食い違い:

- 5: `アスペクト比` / `ﾚﾀｰﾎﾞｯｸｽ透過度` は `LetterBoxView` が実装済みで、**実機で描画されることを確認した**（2026-09-13。`LetterBoxView` + `LetterBoxLine_0/1` がアクティブで、GameView に黒帯が出る）。仕様書の「反映されません」が誤りだったので削除した。`startOffsetTime` は BGM・動画・CSV 出力で使用中。読み手が無いのは `endOffsetTime` / `startFadeTime` / `endFadeTime` のみ
- 6: 保存処理はレイヤーを作らない。実体化の主経路は `操作対象` メイド切替時の暗黙作成（項目 15 と同根）
- 4: MTE も per-key easing による一括補間で、SE の代表タンジェントはその等価変換。ただし SE では値ごとのタンジェントが既に保存されているため、適用側の書き換えだけで個別補間に移行する（XML 不変）

優先度 中の 9〜18 は `docs/superpowers/specs/2026-09-13-timeline-release-debt-medium-design.md` で決定した。調査で判明した仕様書との食い違い:

- 13: `TimelineLayerBase.Init` が新規レイヤーに 0 フレームキーを作っている。エラーになるのはキーを削除・移動した場合と、0 フレームにキーの無い XML を読んだ場合のみ
- 11: ライブ演出は `HistoryManager.BeforeEdit` を呼んでいないだけで、呼べば Undo と自動登録の両方が既存経路で効く

## 実装結果（2026-09-14 追記）

優先度 高・中の実装計画は下記の 2 本で、**両方ともタスクのコミットが揃っている**。ただし計画ファイル内のチェックボックスは未更新のままなので、進捗はコミット履歴で追うこと。

- `docs/superpowers/plans/2026-09-13-timeline-release-debt.md`（高 1〜7）
- `docs/superpowers/plans/2026-09-13-timeline-release-debt-medium.md`（中 1〜9 = 項目 9〜18）

`TimelineData.CurrentVersion` は 34 のままで、XML の要素は増減していない。項目 5 で書き出しを止めた `endOffsetTime` / `startFadeTime` / `endFadeTime` は読み飛ばし扱い（`docs-site/timeline/compatibility.md` に記載済み）。

## 進め方

- 高 1〜8・中 9〜18・低 19〜25 とも決定と実装が完了している。残りは計画ファイルのチェックボックス更新のみ
- 低の実装計画は `docs/superpowers/plans/2026-09-14-timeline-release-debt-low.md`（設計は同名の `specs/` 側）。キー割り当ての移設に伴い、旧 `Timeline.xml` のカスタムキーは既定へ戻る（移行処理は作らないと決定した）
- 見送る項目は `docs-site/timeline/compatibility.md` の「その他の制限」へ明記しておく
