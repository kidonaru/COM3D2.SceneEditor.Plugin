# 一時作業と生成物

`<scene-id>/<task-id>/` の下へ生成XML・検証レポート・試作スクリプトを置く。このREADME以外はGit管理対象外。

`kasou-hiro/key-reduction/` は旧 `work/timeline_key_reduction/` から移した、既存XMLを直接間引いた試行の記録。変換スクリプトの改修に置き換え済みであり、ここにある `reduce_keys.py` や `reduced.xml` を現行処理・最新の原本として使わない。

正式な処理は [../scripts/](../scripts/)、保存するシーン資料は [../scenes/](../scenes/) へ整理する。
