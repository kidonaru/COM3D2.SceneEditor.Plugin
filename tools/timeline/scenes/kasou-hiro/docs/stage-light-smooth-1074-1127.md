# 1074～1127F 回転の連続性修正

2026-09-12、急停止・急な方向切り替えを避ける追加依頼に対応した。先の `stage-light-video-1074-1127.md` の回転設定はこの修正で更新した。

1073→1074F、1127→1128Fの角度差をなくし、その間の回転を端点速度ゼロの三次スプラインへ変更した。1086F・1101Fを中間の姿勢基準とし、終端へは手前から滑らかに戻る。回転の自動補間を対象3成分だけ解除し、入出力の正規化タンジェントを明示した。位置・形状・点灯・色・表示と他のキーは維持した。

ゲームの1フレーム補間は隣接キーを保持する仕様のため、滑らかな曲線は1074～1127Fに置き、1073/1074Fと1127/1128Fを同じ姿勢・速度ゼロとした。キーごとの回転速度をゲーム実行値で照合し、入出力の差の最大は浮動小数点誤差による約0.0224度/秒。端点速度はゼロ。ゲームへ反映し、表示フレームは作業開始時の1112Fへ戻した。

作業ファイルは `tools/timeline/work/kasou-hiro/stage-light-1074-1127/` 内の `smooth.py`、`smooth_boundaries.py`、`validation-smooth.json`、`applied-smooth.json`。適用の入口は `smooth_boundaries.py`。対象は修正前の原本ハッシュに限定する一時処理であり、編集後XMLへ再実行しない。

原本と同じフォルダーに `.bak-smooth-1074-1127-日時` のバックアップを作成した。正確なパスと保存前後のSHA256は `applied-smooth.json` に記録している。
