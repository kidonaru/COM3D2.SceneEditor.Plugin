# 変更履歴

## 2026/08/22 v1.4.0.0

- マイポーズのアニメ再生に対応
  - 保存したポーズも通常のモーションと同じように再生・シークできるようになりました
  - 再生中のエントリを一覧を開かずに前後へ送れるボタンを追加しました
  - 停止とリセットを現在のフレームで行うようになりました
- 適用中のモーションをメイドごとに記録するように変更
  - 再生中の表示と一覧のハイライトが、実際に適用したモーションと一致するようになりました
  - シーンプリセットにも適用したモーションが保存されるようになりました
- 指の編集を改善
  - 開き/握り/ロックをメイドごとに保持するようになり、メイドを切り替えても状態が残ります
  - 指の状態がシーンプリセットにも保存されるようになりました(プリセットv20)
- ボーン編集を改善
  - Inspectorに位置/回転/拡縮の行とフォーカスボタンを追加しました
  - ギズモのLocal/Global切替にスライダーの軸・ラベル・範囲が追従するようになりました
  - スロット選択に「なし」を追加し、ウィンドウを閉じずに骨格線を消せるようになりました
  - ボーン回転がX±90°を超えると値が暴れる問題を修正しました
- ギズモの操作性を改善
  - 操作種別・軸空間・表示対象の切替行をInspectorの先頭に常時表示するようになりました
  - IKやボーンを選んでいるときや未選択のときも、同じ位置でギズモを切り替えられます
  - 表示対象に「すべて表示」「選択中」を追加しました
  - 「すべて表示」では選択していないメイドのギズモも表示され、掴むとそのメイドが選択されます
  - 軸空間(Local/Global)と表示対象を記憶し、次回起動時に前回の状態で開くようになりました
- 外部プラグインとの連携を強化
  - 選択中のモデルを共有するようになりました(ModelSelectHost)
  - ModItemExplorerなどとモデル選択が双方向に連動します
  - ギズモの表示対象を共有するようになりました(GizmoToolClient)
  - 表示対象に対応していない旧版のプラグインとも組み合わせられます
- モーションのカテゴリ一覧が「マイポーズ」だけになることがある問題を修正
- ボーンを選んでいる間にメイドルートのギズモが表示されない問題を修正
- 面ハンドルの内側で軸を掴んでしまう問題を修正
- ドラッグ中に対象が消えると、掴んだギズモが戻らなくなる問題を修正

## 2026/08/20 v1.3.0.0

- 指ウィンドウに「個別編集」トグルを追加。各指関節と指先のドラッグ点で上下＝曲げ・左右＝開きを直接調整でき、その形は指プリセットにも保存される（可動域は指ブレンドのテンプレートから算出しているため逆反りしない。回転を持たない従来のプリセットもそのまま読める）
- 指ウィンドウに部位ごとのフォーカスボタンを追加。指ボーンにカメラを寄せられる
- シーンプリセットの保存・復元を修正。メイドが 1 人も保存されないことがある問題（フォトモードがもともと出しているメイドや、呼出時に既にアクティブだったメイドが漏れていた）と、呼び出したメイドの表情が復元されない問題を解消した。表情ブレンドセット名も保存するようにしたため、まばたきを有効にしたまま保存した表情も再現される（プリセット v19）
- 表情ウィンドウ「目」タブのウィンク系スライダーが実際と食い違っていた問題（「ウィンク左」「ウィンク右」で同じ側の目が閉じ、「半目」「細目」が実際にはもう片方のウィンクだった）と、新ボディ（CRC）の顔で「目閉じ」スライダーが表示されない問題を修正
- シーンビューのカメラ近クリップ面を 0.01 に縮小。注視点の最小距離まで寄せても手前が切れなくなった

## 2026/08/20 v1.2.1.0

- モデルのボーン編集差分をシーンプリセットに保存・復元できるようにした（プリセット v18）。ボーン編集ウィンドウの「モデル」タブで加えた変更が、シーンプリセットの読み込みでそのまま再現される

## 2026/08/20 v1.2.0.0

- ボーン編集ウィンドウに「メイド」「モデル」の対象タブを追加。ModItemExplorer などが配置したモデルのボーンも、メイドと同じようにギズモ・スライダーで編集できる
- モデルのボーン編集も PartsEditWithStudio 互換プリセット（`Config\PartsEdit\*.xml`）で保存・適用できる。PartsEdit で保存したモデル用プリセットもそのまま読める（プリセット一覧は対象種別で自動的に絞り込まれる）
- 外部プラグインが配置モデル一覧を提供するための連携 API（ModelProviderHost）を追加

## 2026/08/19 v1.1.0.0

- ボーン編集にプリセット機能を追加。PartsEditWithStudio と保存先・ファイル形式を共用するため、既存のプリセットをそのまま読み書きできる
- シーンプリセットにスロット別の揺れ物理状態を保存するようにした。プリセットを読み込むと揺れものの状態まで再現される
- 選択中のメイドを外部プラグインと共有する連携 API を追加（ModItemExplorer などと選択が連動する）

## 2026/08/17 v1.0.1.0

- 他人が保存したシーンプリセット（配布サンプル含む）を読み込むと、視線だけが復元されない問題を修正
- ドキュメントサイトを更新（紹介動画の埋め込み、連携対応プラグイン一覧の追加、最近の実装変更への追従）

## 2026/08/16 v1.0.0.0

- 初回リリース
